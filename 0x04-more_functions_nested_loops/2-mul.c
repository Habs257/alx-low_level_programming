#include "main.h"
/**
 * mul - function check for checks for a digit (0 trough
 * @a: a - Variable
 * @b: b - Variable
 * return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul =a * b;
	return (mul);
}
